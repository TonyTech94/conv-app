apiVersion: apps/v1
kind: Deployment
metadata:
  name: conv-api-deployment
  labels:
    app: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: conv-api-container
          image: antonlarichev/conv-api:1.0
          ports:
            - containerPort: 3000
          env:
            - name: AMQP_EXCHANGE
              valueFrom:
                secretKeyRef:
                  name: conv-api-secret
                  key: AMQP_EXCHANGE
            - name: AMQP_HOSTNAME
              valueFrom:
                secretKeyRef:
                  name: conv-api-secret
                  key: AMQP_HOSTNAME
            - name: AMQP_USER
              valueFrom:
                secretKeyRef:
                  name: conv-api-secret
                  key: AMQP_USER
            - name: AMQP_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: conv-api-secret
                  key: AMQP_PASSWORD
            - name: SEEDUSERNAME
              valueFrom:
                secretKeyRef:
                  name: seed-user-secret
                  key: SEEDUSERNAME
            - name: SEEDUSER_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: seed-user-secret
                  key: SEEDUSER_PASSWORD
          volumeMounts:
            - name: uploads
              mountPath: /opt/app/uploads
          resources:
            limits:
              memory: "256Mi"
              cpu: "200m"
      volumes:
        - name: uploads
          persistentVolumeClaim:
            claimName: conv-api-pvc
